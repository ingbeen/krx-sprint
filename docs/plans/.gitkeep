# 이 파일을 지우지 마세요.
#
# 계획서 게이트 훅(.claude/hooks/plan_lint.py)의 project_uses_plans()가
# `docs/plans/` 디렉토리 존재 여부로 "이 프로젝트가 계획서 규약을 채택했는가"를 판정합니다.
# git 은 빈 디렉토리를 추적하지 않으므로, 계획서를 전부 삭제한 뒤 이 파일까지 없으면
# 저장소에서 `docs/plans/` 가 사라지고 새로 clone 한 환경에서 게이트가 통째로 꺼집니다.
#
# 계획서는 임시 산출물이라 주기적으로 비워집니다(docs/ROADMAP.md "계획서의 수명").
# 비울 때 이 파일은 남겨 두세요.
