# krx-sprint 프로젝트 가이드라인

> 루트 문서는 프로젝트 전반의 공통 규칙을 제공하며,
> 각 폴더 문서는 해당 계층의 구체적인 맥락과 핵심 개념을 제공합니다.
> 하위 폴더의 `CLAUDE.md`와 `.claude/rules/`는 해당 경로의 파일을 다룰 때 자동으로 로드됩니다.

## 문서 목적

이 문서는 AI 모델이 krx-sprint 프로젝트를 정확히 이해하고, 일관성 있는 응답을 생성하도록 돕습니다.
사람을 위한 상세 문서가 아닌, AI 모델의 판단 기준과 프로젝트 맥락을 제공합니다.

---

## 목표 주도 실행 (Goal-Driven Execution)

작업을 시작하기 전, 검증 가능한 성공 기준을 정의합니다.

- **모호한 작업을 검증 가능한 목표로 변환한다**:
  - "검증 추가" → "잘못된 입력에 대한 테스트를 작성하고 통과시킨다"
  - "버그 수정" → "버그를 재현하는 테스트를 작성하고 통과시킨다"
  - "X 리팩토링" → "변경 전후 테스트가 모두 통과함을 보장한다"
- **다단계 작업은 단계별 검증 형식으로 제시한다**:

  ```
  1. [단계] → 검증: [확인 방법]
  2. [단계] → 검증: [확인 방법]
  ```

- **약한 성공 기준("동작하게 만들기")은 매 단계 사용자 확인이 필요하다**. 강한 성공 기준은 AI 모델이 독립적으로 루프를 돌릴 수 있게 해준다.

> 보완 관계: 인라인 plan은 즉석 실행용이며, 코드 변경이 필요한 작업은 [`docs/plans/`](docs/plans/)에 공식 계획서를 작성한다 (아래 "계획서(Plan) 작성이 필요한 경우" 섹션 참고).

---

## 규칙 문서 구성

- 공통 규칙: `CLAUDE.md`(루트)
- 계층 규칙: `scripts/CLAUDE.md`(스크립트), `tests/CLAUDE.md`(테스트)
- 경로별 규칙: `.claude/rules/` — 파이썬 구현 원칙·코딩 표준·로깅 정책은 [.claude/rules/python.md](.claude/rules/python.md)
- 절차: `.claude/skills/`
- 개인 작업 철학(사고 절차·수술적 변경 등): 전역 `~/.claude/CLAUDE.md`

> 파이썬 파일을 작성·수정할 때는 [.claude/rules/python.md](.claude/rules/python.md)를 따릅니다.
> 해당 규칙은 파이썬 파일을 다룰 때 자동 로드되지만, 새 파일을 만드는 등 자동 로드가 없는 경우에는 직접 읽습니다.

---

## 계획서(Plan) 작성이 필요한 경우

원칙: 모든 코드 변경은 계획서를 작성한 후 진행합니다.

예외 (계획서 없이 바로 진행 가능):

- 오타 수정
- 주석 수정
- 로그 메시지 수정

위 예외를 제외한 모든 변경은 `docs/plans/`에 계획서를 작성해야 합니다.

계획서 작성 절차 및 품질 게이트: `/plan` 스킬([.claude/skills/plan/SKILL.md](.claude/skills/plan/SKILL.md))이 SoT입니다.

이 규칙은 하네스가 강제합니다 (`.claude/hooks/`).

- 문서(`docs/**`, `**/*.md`)를 제외한 파일을 편집할 때 계획서가 선행되지 않으면 권한 프롬프트가 뜹니다.
  차단이 아니라 확인이며, 세션당 한 번만 걸립니다. 오타·주석·로그 수정이면 승인하면 됩니다.
- Done 조건을 위반한 계획서는 저장이 차단됩니다. Bash(`sed`·heredoc 등)를 경유해도 검사합니다.

### 계획서의 수명 (CRITICAL)

계획서는 **작업 중에만 존재하는 임시 산출물**입니다. 사용자가 주기적으로 `docs/plans/`를 전부 비웁니다.

- **살아있는 문서에서 계획서를 링크하지 않습니다.** 링크하면 다음 삭제 주기에 깨집니다
- **Done 처리 시 남길 가치가 있는 것을 살아있는 문서로 승격합니다.** 결론뿐 아니라 **근거**("왜 그 값인가")를
  옮깁니다. 승격 대상과 목적지는 [docs/ROADMAP.md](docs/ROADMAP.md) "계획서의 수명"이 SoT입니다
- `docs/plans/.gitkeep`은 **지우지 않습니다** — 디렉토리가 사라지면 하네스의 계획서 게이트가 꺼집니다

---

## 외부 라이브러리/도구 학습

작업에 필요한 외부 라이브러리/도구의 최신 사용법, 버전 차이, 공식 스펙 확인이 필요한 경우:

1. **Context7으로 먼저 학습한다**.
2. 부족하거나 불명확한 부분이 있으면 **웹검색을 활용해 근거를 보완한다**.

특히 `pykrx`는 KRX 웹을 래핑하는 라이브러리라 동작이 조용히 바뀔 수 있다.
스펙 문서([docs/데이터수집_스펙_v2.md](docs/데이터수집_스펙_v2.md))의 "실측 검증" 항목은 기억이 아니라 실측으로 확정한다.

---

## 프로젝트 개요

krx-sprint는 KRX(코스피+코스닥) 주도테마 대장주 단기매매 전략을 위한 프로젝트입니다.

- **현재 단계**: 데이터 수집·검증과 백테스트 엔진이 모두 구현돼 있고, **첫 전략은 백테스트 결과 기각**됐습니다. 진행 상태의 SoT는 [docs/ROADMAP.md](docs/ROADMAP.md)이며, 기각 근거는 [docs/백테스트_설계_v1.md](docs/백테스트_설계_v1.md) §14에 있습니다
- **설계 SoT**: 수집은 [docs/데이터수집_스펙_v2.md](docs/데이터수집_스펙_v2.md)(실측 기록), 백테스트는 [docs/백테스트_설계_v1.md](docs/백테스트_설계_v1.md)
- **전략 요약**: 평균회귀 눌림목 — 주도테마의 대장주를 첫 하락에서 매수, 반등에서 익절. 판단은 일봉, 집행은 예약매매 (look-ahead 금지)

핵심 설계 원칙 (스펙 문서에서 상세 확인):

- **Point-in-Time**: 모든 데이터는 과거 시점 값 그대로 (미래 정보 오염 금지)
- **생존편향 방지**: 일별 전종목 스냅샷의 합집합으로 유니버스 구성 (폐지 종목 보존)
- **원본가/수정주가 분리**: 폴더 단위로 분리 저장, 혼용 금지

기술 환경:

- Python 3.12 (`str | None` 문법 사용)
- 의존성 관리: Poetry
- 코드 품질: Black, Ruff
- 타입 체커: PyRight (strict mode for src/)
- 주요 라이브러리: pandas, pykrx, pyarrow

---

## 디렉토리 구조

```
krx-sprint/
├── .claude/                 # 하네스 설정 (규칙 강제 계층)
│   ├── hooks/               # 계획서 게이트·불변조건 검사 훅
│   ├── rules/               # 경로별 자동 로드 규칙
│   └── skills/              # 절차 스킬 (계획서 작성 등)
├── src/krx_sprint/          # 비즈니스 로직 패키지
│   ├── common_constants.py  # 공통 상수 (경로, 컬럼명, 수집 기간 등)
│   ├── collect/             # 1·2단 수집기와 품질 검증 로직
│   ├── panel/               # 1·2단을 조인한 백테스트용 통합 패널 빌더·로더
│   ├── backtest/            # 신호·체결·엔진·비용 모델·성과 지표
│   └── utils/               # 공통 유틸리티 (로거, 포맷팅, CLI 헬퍼, 메타 관리)
├── scripts/                 # CLI 스크립트 (상세: scripts/CLAUDE.md)
│   ├── data/                # 수집·검증·패널 빌드 스크립트
│   ├── backtest/            # 백테스트 실행 스크립트
│   └── adhoc/               # 일회성 진단 스크립트
├── tests/                   # 테스트 코드 (상세: tests/CLAUDE.md)
├── docs/                    # 프로젝트 문서 및 계획서
│   ├── 데이터수집_스펙_v2.md  # 수집 단계 실측 기록 (SoT)
│   ├── 백테스트_설계_v1.md    # 전략 설계와 기각 근거 (SoT)
│   ├── ROADMAP.md           # 구현 순서 및 진행 상태
│   ├── COMMANDS.md          # 실행 명령어 단일 관리
│   └── plans/               # 작업 계획서 (임시 산출물, 주기적으로 비움)
└── storage/                 # 데이터 저장소 (구조: 스펙 §7.2, 1단만 git 동기화)
```

---

## 실행 명령어 관리 원칙

> CRITICAL: 모든 실행 명령어(`poetry run` 등)는 **[docs/COMMANDS.md](docs/COMMANDS.md)에서 단일 관리**합니다.
> CLAUDE.md 파일에는 실행 명령어를 기재하지 않으며, 필요 시 `docs/COMMANDS.md` 를 참조합니다.

## 스크립트 실행 규칙

> CRITICAL: `scripts/` 폴더에 위치한 모든 스크립트는 **사용자가 직접 실행**하는 것이 기본입니다.
> AI 모델의 직접 실행이 필요한 경우에는 사용자에게 요청해 승인을 받은 뒤에만 실행합니다.

- AI 모델의 역할: 스크립트 코드 작성/수정, 실행 방법 안내
- 사용자의 역할: 스크립트 직접 실행, 결과 확인 및 피드백
- 예외: `validate_project.py`는 루트에 위치하며 AI 모델이 실행 가능
- 실행 요청: 로그 분석 등 AI 모델이 직접 실행해야 판단할 수 있는 경우, 요청 규모와 사유를 밝히고 승인을 받습니다

특히 수집 스크립트는 KRX 서버에 실제 요청을 보내므로 (레이트리밋·차단 위험) 기본적으로 사용자가 실행합니다.

---

## 개발 원칙

1. YAGNI: 필요성이 확인될 때 구현
2. 간결성: 불필요한 추상화 지양
3. 확장성: 도메인별 모듈 독립성 유지
4. 사용자 중심: 한글 메시지, 명확한 오류 정보
5. 자문 체크: "시니어 엔지니어가 과복잡하다고 볼까?" — 그렇다면 단순화
