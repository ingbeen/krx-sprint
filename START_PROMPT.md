# 다음 세션 시작 프롬프트

> 아래 "---" 이후 내용을 복사해 새 세션 첫 메시지로 붙여넣으세요.
> (이 파일은 안내용이며, 세션을 시작한 뒤 삭제해도 됩니다)

---

이 폴더는 krx-sprint — KRX(코스피+코스닥) 주도테마 대장주 단기매매 프로젝트다.
데이터 수집·검증이 끝났고, 이번 세션에서는 **백테스트 단계(ROADMAP Phase 6)**를 시작한다.

먼저 다음 문서를 순서대로 읽어라:

1. `CLAUDE.md` (루트) — 프로젝트 공통 규칙. 여기 나온 폴더별 CLAUDE.md 참고 규칙도 준수할 것
2. `docs/ROADMAP.md` — **진행 상태 문서**. 현재 위치, 완료된 Phase의 산출물,
   "실측으로 확정된 것(재조사 불필요)", 백테스트로 넘겨야 할 처리 규칙이 정리돼 있다
3. `docs/데이터수집_스펙_v2.md` — 데이터 수집 설계 확정본(SoT).
   **§0 실측 결과, §3.1 외부 대조 결과, §8.1·§8.2 검증 결과가 핵심**
4. `docs/COMMANDS.md` — 실행·진단 명령어 단일 관리
5. `docs/plans/` — 완료된 계획서 5건. 각 진행 로그에 결정 근거와 발견 사항이 남아 있으니
   같은 판단을 다시 하지 말 것

## 확보된 데이터

- **1단 일별 전종목 스냅샷** `storage/snapshots/{YYYY}/{YYYYMMDD}.parquet`
  2019-01-01 ~ 2026-07-27 / 1,857일 / 4,702,643행 / **원본가**
- **2단 종목별 수정주가** `storage/adjusted/{ticker}.parquet`
  3,136종목 / 4,728,694행 / **수정주가** (git 제외 — 1단만 있으면 약 1시간에 재생성)
- 검증 상태: 1단 품질 리포트·2단 정합성 리포트 모두 **오류 0건**.
  폐지 371종목 전부 보존돼 생존편향이 2단까지 차단됨. 티커 재사용 0건
- 외부 대조: 삼성전자 2026-07-21~24의 OHLC와 거래량이 외부 시세와 **완전 일치**.
  액면분할 사례(`025560`)는 DART 공시와 주식수·일정까지 일치. 앵커 4건이 자동 검사에 등록돼 있다
- 환경: pyenv(Python 3.12) + Poetry(`.venv` 프로젝트 내부). KRX 자격증명은 `.env` 단일 출처
- `poetry run python validate_project.py` 통과 (passed=213, failed=0, skipped=0)

## 이번 세션에서 할 일

**ROADMAP Phase 6 — 백테스트.** 스펙 §11이 "별도 설계 문서 작성 후 진행"으로 규정했으므로
곧바로 코딩하지 말고 **설계 문서를 먼저 작성해 내 검토를 받아라.**

설계 문서에 담아야 할 것:

- 테마 동조화(co-movement) 클러스터링 — 외부 테마 분류는 미래참조 편향이라 쓰지 않는다
- 기준봉 정의(거래대금 급증 × 상승률), N차 하락 카운팅, 3차 하락 이상 진입 금지
- 손절(진입 스윙 전저점 / 5·10일선 / 고정 %)과 익절(손익비 역산)
- 비용 모델 — 수수료 + 증권거래세 + 슬리피지. **세율은 연도별·시장별로 다르고 기간 중 여러 번 바뀌었다.
  기억으로 쓰지 말고 출처를 확인해 확정할 것**
- 체결 가정 — 예약(지정가) 미체결, 상한가 마감, 갭
- 데이터 접근 계층 — 백테스트는 "전 기간 × 전종목" 패널이 필요한데 현재 로더는 파일 단위다.
  통합 캐시(`storage/cache/`) 스키마를 여기서 확정한다

설계가 확정되면 `/plan` 스킬 규칙에 따라 `docs/plans/`에 계획서를 쓰고 구현에 들어간다.

## 백테스트가 반드시 반영해야 할 처리 규칙

수집 단계에서 실측으로 확인해 스펙에 기록해 둔 것이다. 상세는 스펙 §8.1·§8.2.

| 규칙 | 실측 규모 | 미적용 시 |
|---|---|---|
| 거래정지일(거래량 0) 매매 불가 | 169,623행 (3.61%) | 체결 불가능한 거래를 체결로 가정 |
| 상장주식수 급변일을 기준봉 판정에서 제외 | 3,345건 | 감자·분할을 급등으로 오인 (최대 +29,948%) |
| 수정 미반영 액션일을 수익률·스윙에서 제외 | 16건 | 최대 300배 가짜 갭 |
| 1단 최초 등장일 이전 구간 절단 | 43종목 28,811행 | 최대 −82% 가짜 수익률 |
| 정규장 미형성(`거래량 > 0 AND 저가 = 0`)을 고저 계산에서 제외 | 11행 | 가짜 전저점 |

절단은 `load_adjusted(ticker, since=...)`에 1단 최초 등장일을 넘겨서 한다
(기준 일자는 `snapshot_store.list_first_seen_dates()`).

**원본가와 수정주가를 혼용하지 말 것** — 거래대금·스크리닝은 1단(원본), 지표·수익률은 2단(수정주가)이다.
특히 **2단의 거래량은 조정돼 있으므로**(1,147종목) 거래량·거래대금 계산에 쓰면 안 된다.

## 재조사하지 말 것

ROADMAP "실측으로 확정된 것"에 적힌 pykrx·KRX 동작은 이미 실측으로 확정됐다.
로그인 필수, 휴장일 응답 형태, 가격 0 판정, 당일 데이터, `market="ALL"`,
스크리닝 무력화, 티커 영숫자, 수정계수 조회시점 기준, KRX 미조정 액션, 2단 ⊃ 1단 — 전부 근거가 문서에 있다.

## 지켜야 할 것

- **스크립트 실행은 네가 해도 된다** (이전 세션에서 위임함). 단 `scripts/data/collect_*.py`는
  KRX에 실제 요청을 보내므로 레이트리밋을 고려해 필요한 최소 범위로만 돌릴 것
- **Git 처리는 내가 직접 한다.** 커밋·푸시하지 말 것
- **데이터 보간·수정 금지.** 이상을 발견하면 재수집하거나 처리 방침을 스펙에 기록한다
- 1단 스냅샷 파일은 **불변**이다. 재수집이 필요하면 파일을 지운 뒤 수집기를 다시 실행한다.
  2단은 가변이다(수정주가는 과거가 재계산된다)
- 코드 변경 전에는 `docs/plans/`에 계획서를 쓰고 내 검토를 받는다 (오타·주석·로그 메시지 수정은 예외)

## 미완료로 남아 있는 것 (필요해지면 착수)

- **ROADMAP Phase 4 — 증분 업데이트**: 매일 갱신 자동화, 당일 확정 시각 실측
  (현재 17:00은 보수적 추정값), **2단 갱신 정책 미정**(매일 전량 재수집 1시간은 비현실적이라
  액션 발생 종목만 선별 갱신하는 설계가 필요). 실전 운용 전에는 반드시 필요하다
- **PIT 종목명**: `names.csv`는 최신 시점 매핑만 담는다.
  `get_market_sector_classifications(date, market)`로 소급 가능함이 확인돼 있다
- **관리종목·투자경고 지정 이력**: 미수집. 필터 실험을 하려면 별도 수집이 필요
- **이전상장 구간의 시장 확정**: 코넥스로 추정되나 미확정. 판정 로직은 시장명에 의존하지 않는다
